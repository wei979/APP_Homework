/// 圓角半徑，對應設計稿 `--radius-*` token。
class AppRadius {
  AppRadius._();

  static const double xs = 4;
  static const double s = 8;
  static const double m = 12;
  static const double l = 16;
  static const double xl = 28;
}

/// 共用間距 / 尺寸常數。
class AppGap {
  AppGap._();

  static const double touchTarget = 48; // Material 觸控標準 ≥ 48dp
}